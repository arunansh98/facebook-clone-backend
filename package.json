{
  "name": "facebook-clone-backend",
  "version": "1.0.0",
  "description": "Backend for Facebook clone using json-server-auth",
  "main": "server.js",
  "scripts": {
    "start": "json-server-auth db.json -r routes.json --port $PORT"
  },
  "dependencies": {
    "json-server": "^0.17.4",
    "json-server-auth": "^2.1.0"
  },
  "engines": {
    "node": ">=14"
  },
  "license": "MIT"
}
